# الأدب العربي في البكالوريا 2.0 — الواجهة المتصلة

تم في هذه المرحلة ربط فضاء التلميذ وفضاء الأستاذ بالقلب المشترك D1/API مع إبقاء الوضع المحلي كشبكة أمان عند تعذر الاتصال.

## ما أصبح مشتركًا
- تسجيل حسابات التلاميذ وتسجيل الدخول.
- قائمة التلاميذ وحالات الحساب والتقدم.
- الرسائل بين الأستاذ والتلميذ.
- المهمات والإنتاجات والمراجعات.
- الاختبارات والأسئلة وإجابات التلاميذ.
- ملاحظات الأستاذ وردود التلاميذ.
- الإعلانات والتنبيهات.
- روابط الوسائط الخارجية.
- إعدادات المنصة وسجل النشاطات.
- المحتوى الأساسي 147 موردًا تتم تهيئته تلقائيًا في D1 عند أول دخول للأستاذ.
- إنشاء محتوى جديد وتعديله ونشره/إخفائه من لوحة الأستاذ.

## ملاحظة مهمة
الاحتفاظ بـ localStorage في الواجهة مقصود كـ fallback فقط. عند توفر جلسة API صالحة تكون البيانات الأساسية من D1 هي المصدر المشترك.

## النشر
1. افتح PowerShell داخل مجلد `worker`.
2. نفّذ `npx.cmd wrangler login` إذا كان تسجيل الدخول يعمل، أو استخدم إعداد المصادقة المناسب في بيئتك.
3. اضبط أسرار Worker:
   - `npx.cmd wrangler secret put SESSION_SECRET`
   - `npx.cmd wrangler secret put TEACHER_USER`
   - `npx.cmd wrangler secret put TEACHER_PASS`
4. نفّذ `npx.cmd wrangler deploy`.
5. اختبر `https://arabya.aboufirasdridi.workers.dev/api/health`.
6. سجّل تلميذًا من هاتف، ثم ادخل إلى فضاء الأستاذ وتحقق من ظهوره.

## النسخة الحية 1.0
لم تُعدّل. هذه الحزمة مستقلة للتطوير والنشر التجريبي لـ2.0.
